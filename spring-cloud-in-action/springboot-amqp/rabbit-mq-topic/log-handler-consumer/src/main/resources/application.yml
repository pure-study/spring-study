spring:
  application:
    name: log-handler-consumer

  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: will
    password: will1234

server:
  port: 9003

mq:
  config:
    exchange:
      name: log.topic   # exchange name
    queue:
      logs:
        name: log.all
      info:
        name: log.info
      error:
        name: log.error
