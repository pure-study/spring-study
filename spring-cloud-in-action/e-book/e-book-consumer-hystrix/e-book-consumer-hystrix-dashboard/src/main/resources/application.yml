spring:
  application:
    name: e-book-hystrix-dashboard

server:
  port: 1001

eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"
